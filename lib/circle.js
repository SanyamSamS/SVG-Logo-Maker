const Shape = require('./shapes');

class Circle extends Shape {
    constructor(shape, color) {
        super('circle', color);
    }

    render() {
        return circle
    }
}

module.exports =  Circle